This app displays the current bitcoin price from an Api using Axios , displays it and takes in a target price as input and triggers a desktop notification when the price <br>
reaches the target price
